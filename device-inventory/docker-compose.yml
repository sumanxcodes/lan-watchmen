services:
  device-inventory:
    build: .
    container_name: device-inventory
    # Use host networking so arp -a sees the Docker VM host's interfaces
    network_mode: "host"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
